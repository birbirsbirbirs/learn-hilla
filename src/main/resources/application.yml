server:
  port: ${PORT:8080}
logging:
  level:
    org.atmosphere: warn
spring:
  mustache:
    check-template-location: false
  jpa:
    defer-datasource-initialization: true
vaadin:
  launch-browser: true
  whitelisted-packages: com.vaadin,org.vaadin,dev.hilla,co.ptm.learn_hilla

